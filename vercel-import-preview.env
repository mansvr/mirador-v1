# Import in Vercel → Settings → Environment Variables → Import .env
# Target: Preview only → check "All Previews" / all branches (not Production).
#
# NEXT_PUBLIC_SITE_URL is optional on Preview: getSiteUrl() uses the deployment host
# (e.g. mirador-v1-git-main-mansurme.vercel.app) when unset.
# Set it only if embed/OG must point at a fixed hostname.

NEXT_PUBLIC_R2_URL=https://pub-709488f795594fd1bbdf827d49a5ff63.r2.dev
